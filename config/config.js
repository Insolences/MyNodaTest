module.exports = {
    secret: "SECRET_KEY_RANDOM"
}



  // [
  // check('first_name', 'Имя пользователя не должно быть пустым').notEmpty().isLength({ min: 3, max: 10}),
  //   check('last_name', 'Имя пользователя не должно быть пустым').notEmpty().isLength({ min: 3, max: 10}),
  //   check('email', 'Укажите корректный email').isEmail(),
  //   check('password', 'Длинна должна быть больше 6 символов').notEmpty().isLength({ min: 6, max: 12}),
  // ]
